name: Build Docker Image
on:
  push:
    branches:
      - dev
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Build Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: false
          tags: todo:latest
          # Hier fehlt leider der richtige Tag 
          # Die Aufgabe war, das Image in GitHub Packages zu pushen
          # D.h., dass zum einen ein Login erfolgen muss, zum anderen der Tag "ghcr.io/<org name>/..." sein muss
